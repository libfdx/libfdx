package io.github.libfdx.ecs.scene;

/** Stable identifiers for the core portable ECS scene document format. */
public final class EcsSceneFormat {
    /** Scene document format identifier. */
    public static final String FORMAT = "libfdx.ecs.scene";

    /** Current scene document version. */
    public static final int VERSION = 1;

    private EcsSceneFormat() {
    }
}
