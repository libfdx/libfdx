package io.github.libfdx.tests.graphics;

/**
 * Keeps the legacy `fog-2d` selector on the 2D fog-of-war scenario.
 *
 * @author xpenatan
 */
public final class Fog2DTest extends FogOfWar2DTest {
    /**
     * Creates a 2D fog test.
     *
     * @param exitAfterFrames the exit after frames
     */
    public Fog2DTest(long exitAfterFrames) {
        super(exitAfterFrames);
    }
}
