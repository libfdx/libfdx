name: Pull Request

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    name: Build Pull Request
    uses: ./.github/workflows/workflow_build_and_upload.yml
    with:
      isRelease: false
      shouldUpload: false
